/**
 * This is a single-user personal app — no auth flow yet. The seed script
 * creates this user and the API composition root resolves "the current
 * user" to this row until multi-user auth is introduced.
 */
export const DEFAULT_USER_EMAIL = "me@english-a1.local";
